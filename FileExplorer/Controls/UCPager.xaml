<UserControl x:Class="FileExplorer.Controls.UCPager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileExplorer.Controls"
             mc:Ignorable="d" 
             Name="ucPager"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <TextBlock Text="{Binding Path=ItemLen,ElementName=ucPager,StringFormat={}{0:共0条数据}}" HorizontalAlignment="Left"/>
        </Grid>
        <Grid Grid.Column="1" Name="gridPager">
            <StackPanel Orientation="Horizontal" Name="panelPager" HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</UserControl>
