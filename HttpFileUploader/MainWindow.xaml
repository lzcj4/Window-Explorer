<Window x:Class="HttpFileUploader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HttpFileUploader"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Text="文件名："/>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding FilePath}" />
        <Button Grid.Column="2" Grid.Row="0" Content="Open"
                Margin="10,0,0,0" Command="{Binding OpenCommand}" />

        <Button Grid.Column="1" Grid.Row="1" Content="Upload" Width="60" Height="25"
                Command="{Binding UploadCommand}" />
    </Grid>
</Window>
